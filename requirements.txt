# ── IA / API ──────────────────────────────────────────────
anthropic>=0.40.0

# ── Vidéo ─────────────────────────────────────────────────
moviepy>=2.0.0
Pillow>=10.0.0
numpy>=1.24.0

# ── Automatisation navigateur (mode Playwright TikTok) ────
playwright>=1.40.0

# ── Réseau / HTTP ─────────────────────────────────────────
httpx>=0.27.0

# ── Utilitaires ───────────────────────────────────────────
python-dotenv>=1.0.0

# ── Audio (optionnel : pydub pour manipulation avancée) ───
# pydub>=0.25.1

# ── Voix off hors ligne ───────────────────────────────────
# Dépendance système (pas pip) : espeak-ng
# Ubuntu/Debian : sudo apt install espeak-ng
# Mac           : brew install espeak-ng
# Windows       : https://github.com/espeak-ng/espeak-ng/releases
