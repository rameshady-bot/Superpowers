# Core async HTTP
httpx>=0.27.0
httpx[http2]>=0.27.0

# Environment
python-dotenv>=1.0.0

# Anthropic (Claude) — script generation
anthropic>=0.30.0

# Audio — ElevenLabs
elevenlabs>=1.3.0

# Speech-to-text — Whisper (local, no API cost)
openai-whisper>=20231117
torch>=2.2.0           # Required by Whisper
torchaudio>=2.2.0

# Video processing — FFmpeg Python wrapper
ffmpeg-python>=0.2.0

# Database
# sqlite3 is stdlib — no extra package needed

# CLI
click>=8.1.7

# Utilities
tenacity>=8.3.0        # Retry logic with exponential backoff
rich>=13.7.0           # Pretty console output and progress bars
pydantic>=2.7.0        # Data validation for API responses
python-dateutil>=2.9.0

# Logging / cost tracking
structlog>=24.2.0
