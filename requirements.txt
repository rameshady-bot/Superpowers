# Core async HTTP
httpx>=0.27.0
httpx[http2]>=0.27.0

# Environment
python-dotenv>=1.0.0

# Anthropic (Claude) — script generation (~$0.002/video, negligible)
anthropic>=0.30.0

# ── Audio ──────────────────────────────────────────────────────────────────────
# ElevenLabs — free tier: 10 000 chars/month (optional, falls back to gTTS)
elevenlabs>=1.3.0

# gTTS — Google Text-to-Speech, completely free and unlimited fallback
gTTS>=2.5.0

# ── Video (local free option) ──────────────────────────────────────────────────
# SadTalker runs via subprocess (cloned separately — see README_TIKTOK.md)
# No pip package needed — just set SADTALKER_PATH in .env

# ── Speech-to-text ─────────────────────────────────────────────────────────────
# Whisper — local, free, no API cost
openai-whisper>=20231117
torch>=2.2.0
torchaudio>=2.2.0

# ── Video processing ───────────────────────────────────────────────────────────
ffmpeg-python>=0.2.0

# ── CLI & utilities ────────────────────────────────────────────────────────────
click>=8.1.7
tenacity>=8.3.0
rich>=13.7.0
pydantic>=2.7.0
python-dateutil>=2.9.0
structlog>=24.2.0
